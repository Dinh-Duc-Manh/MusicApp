import 'package:flutter/material.dart';

class FavoriteTad extends StatelessWidget {
  const FavoriteTad({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('favorite Tab'),
      ),
    );
  }
}
